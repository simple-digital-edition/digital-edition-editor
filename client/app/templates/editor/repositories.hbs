<div class="cell">
  <h1>Select the Project to load</h1>
  <ul class="menu vertical" role="menubar">
    {{#each model as |repository|}}
      <li role="menuitem"><a href="#" {{action "select-repository" repository.id}}>{{repository.title}}</a></li>
    {{/each}}
  </ul>
</div>
