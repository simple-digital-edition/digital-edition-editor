import prosemirrorEditor from 'client/utils/prosemirror-editor';
import { module, test } from 'qunit';

module('Unit | Utility | prosemirror-editor', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = prosemirrorEditor();
    assert.ok(result);
  });
});
